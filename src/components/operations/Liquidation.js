import React from 'react'
import {FormattedMessage} from 'react-intl'

// no props - just runs inflation
const Liquidation = () => <FormattedMessage id="operation.liquidation" />

export default Liquidation
